我希望实现如下功能
1. 点击data-table-toolbar按钮，以popover的方式弹出一个form表单，表单中有3个tab页，表单的最下面有”确定“按钮和取消按钮 ，点击确定按钮后关闭form表单并触发后台接口（这个接口暂时不需要帮我实现，用alert代替即可），点击取消按钮后关闭form表单并且不做任何操作
2.第1个tab页展示一个筛选表单，表单中的每一行元素可以通过最后的按钮”添加条件“来添加，每一行元素包括3列，第一列为select，可以选择筛选的字段，第二列筛选的条件（包括等于、不等于、包含、不包含、空、不为空），第三列为输入框，可以输入对应的值
3.第2个tab页展示一个排序表单，表单中的每一行元素可以通过最后的按钮”添加排序“来添加，每一行元素包括2列，第一列为select，可以选择排序的字段，第二列为select，可以选择升序或降序
4. 第3个tab页展示一个分组表单，表单中的每一行元素可以通过最后的按钮”添加分组“来添加，每一行元素包括2列，第一列为select，可以选择分组的字段，第二列为select，可以选择升序或降序
5. 三个表单内的每一行元素都可以通过拖动来调整顺序，拖动的位置为每一行元素的第一列前面的一个图标，这个图标是一个拖动的图标，用lucide图标库中的drag图标即可


我希望实现view的重命名功能，具体如下：
1. 点击这个view对应的重命名按钮，弹出一个输入框，输入框中的内容为当前view的名称
2. 输入框中的内容可以修改，修改后点击确定按钮，关闭输入框，同时调用view-services.tsx中的updateViewName方法修改view的名称

我希望在task的view tab的最右侧添加一个按钮，点击这个按钮可以弹出一个form表单，表单中有一个输入框，输入框中可以填写view的名称，表单的最下面有”确定“按钮和取消按钮 ，点击确定按钮后关闭form表单并触发后台接口（这个接口暂时不需要帮我实现，用alert代替即可），点击取消按钮后关闭form表单并且不做任何操作


## 我希望在task页面中实现如下功能，请尽量用中文回答我
根据task的view tabs中的view对应的conditions，来对data-table中的数据进行筛选，排序（先不考虑分组），具体如下：
- 当用户点击任意一个view-tab的时候，根据view.conditions配置对useViews中返回的数据进行筛选和排序，输出筛选排序之后的数据作为数据源展示到data-table中。展示的方式和分页还是按照当前代码中的逻辑来。你需要重点关注的是筛选和排序的逻辑，也就是对给到data-table的数据进行提前处理的逻辑。
- 筛选需要根据conditions.filters中的筛选条件，对data-table中的数据进行筛选，筛选条件包括等于、不等于、包含、不包含、空、不为空等。可以多个字段进行筛选，每个字段的筛选条件可以不一样，按照conditions.filters数组中的顺序为优先级进行筛选。
- 排序需要根据conditions.groups条件，对data-table中的数据进行排序，排序条件包括升序和降序，可以多个字段进行排序，每个字段的排序方式可以不一样。按照conditions.groups数组中的顺序为优先级进行排序。

## 请注意
- 页面初始化的时候默认选中第一个tab页，以及切换tab页时，也需要根据选中的view对data-table中的数据进行重新筛选和排序
- 如果useViews中的数据发生变化，也需要重新对data-table中的数据进行重新筛选和排序
- 如果useViews中没有返回任何数据，则不需要对data-table中的数据进行筛选和排序，按照空table展示即可
- 如果筛选和排序的字段在data-table中不存在，则忽略该字段即可

请帮我实现上面的功能，实现的时候尽量做好模块拆分和优化，方便后续的维护和扩展，谢谢！


我希望在将当前的view中的conditions展示到data-table-toolbar的筛选表单中，请帮我实现这个功能，建议你按照如下方式来思考实现
1. 将currentView传递到data-table-toolbar
2. 在defaultValues中将currentView中的conditions赋值给对应的form
3. 你暂时无需处理onSubmit中将form表单中的已经修改过的数据重新赋值给currentView中的conditions
